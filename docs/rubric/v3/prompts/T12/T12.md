# T12: SoulEditor 组件

## 需求量表自检
- R1: 5 — 对照 V1 ui/SoulEditor.ts (1266行) + V2 (648行)
- R2: 5 — V1 存在
- R3: 5 — Props/Emits 对照 V2
- R4: 5 — 完整
- R5: 5 — 单一职责
- **总分: 5.0**

## 任务
创建 `v3/components/SoulEditor.vue`。

## 上下文

### Props（对照 V2 SoulEditor.vue）
```typescript
defineProps<{
  modelValue?: {
    identity: string;
    goals: string[];
    personality: string;
  };
}>();
```

### Emits（对照 V2）
```typescript
defineEmits<{
  'update:modelValue': [value: { identity: string; goals: string[]; personality: string }];
  change: [value: { identity: string; goals: string[]; personality: string }];
}>();
```

### 功能（来源：V1 ui/SoulEditor.ts）
1. 身份描述编辑（textarea）
2. 目标列表（添加/删除/排序）
3. 性格特征编辑（textarea）
4. 模板选择（预设 Soul 模板）
5. 导出/导入

### 模板来源确认
```bash
grep -rn "SoulTemplate\|soulTemplate" src/stratix-character-creator/
```
使用 `SoulTemplateRenderer` 如果存在：
```typescript
import { soulTemplateRenderer } from '@/stratix-character-creator/core/SoulTemplateRenderer';
```

## 验收标准
- [ ] Props/Emits 匹配
- [ ] identity/goals/personality 编辑
- [ ] 模板选择
- [ ] `var(--ds-*)` tokens
- [ ] `npx tsc --noEmit` 通过

## 禁止事项
- 禁止导入 Phaser
- 禁止修改 core/
