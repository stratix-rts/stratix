# T08: useCharacterState composable

## 需求量表自检
- R1: 5 — 对照 V2 useCharacterState.ts (247行) + V1 Scene saveCharacter
- R2: 5 — 核心 CRUD
- R3: 5 — 接口对照 V2
- R4: 5 — 含 DataCloneError 修复经验
- R5: 5 — 单一职责
- **总分: 5.0**

## 任务
创建 `v3/composables/useCharacterState.ts`。

## 上下文

### V2 参考（来源：`src/components/character-creator/v2/composables/useCharacterState.ts`，247 行）

### 关键依赖
```typescript
import { ref, computed, toRaw } from 'vue';
import type { Ref } from 'vue';
import type { SavedCharacter, BodyType } from '@/stratix-character-creator/types';
import { characterStorage } from '@/stratix-character-creator/core/CharacterStorage';
import { characterComposer } from '@/stratix-character-creator/core/CharacterComposer';
import { textureManager } from '@/stratix-character-creator/core/TextureManager'; // 检查是否实际存在
```

### 返回接口（对照 V2 行 24-50）
```typescript
return {
  currentCharacter,    // Ref<SavedCharacter | null>
  savedCharacters,     // Ref<SavedCharacter[]>
  isDirty,             // ComputedRef<boolean>
  hasCharacter,        // ComputedRef<boolean>

  loadSavedCharacters, // () => Promise<void>
  createNew,           // (bodyType?) => SavedCharacter
  loadCharacter,       // (id: string) => Promise<SavedCharacter | null>
  saveCharacter,       // () => Promise<boolean>
  deleteCharacter,     // (id: string) => Promise<boolean>
};
```

### ⚠️ DataCloneError 修复（上一轮踩坑）
保存到 IndexedDB 时，Vue reactive proxy 不可序列化：
```typescript
// 正确做法：深拷贝剥离 reactive proxy
const toSave: SavedCharacter = JSON.parse(JSON.stringify(toRaw(character)));
```

### 保存流程（来源：V1 Scene saveCharacter，行 1331-1408）
1. composeCharacter → canvas
2. generateThumbnail → thumbnail
3. generateAndUploadTexture → texture
4. updatedAt = Date.now()
5. 深拷贝 + 清理 apiKey
6. characterStorage.save()
7. isDirty = false
8. loadSavedCharacters() 刷新列表

## 验收标准
- [ ] 接口与 V2 一致
- [ ] DataCloneError 已预防（JSON.parse(JSON.stringify(toRaw()))）
- [ ] 保存流程完整 8 步
- [ ] apiKey 清理
- [ ] `npx tsc --noEmit` 通过

## 禁止事项
- 禁止用 spread 浅拷贝（必须深拷贝）
- 禁止修改 core/
