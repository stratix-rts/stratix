# T15: AgentConfigStep 容器

## 需求量表自检
- R1: 5 — 对照 V1 ui/AgentConfigPanel.ts (1543行) + V2 AgentConfigStep.vue
- R2: 5 — V1 存在
- R3: 5 — Props/Emits 对照 V2
- R4: 5 — 整合组件
- R5: 4 — 容器组件
- **总分: 4.85**

## 任务
创建 `v3/components/AgentConfigStep.vue` — 整合 Backend + Soul + Rules + SkillTree。

## 上下文

### V1 参考
- `src/stratix-character-creator/ui/AgentConfigPanel.ts` (1543行)

### Props（对照 V2 AgentConfigStep.vue）
```typescript
defineProps<{
  modelValue?: {
    backendType: AgentBackendType;
    openClawConfig?: OpenClawConfigLocal;
    stratixConfig?: StratixDirectConfig;
    soul?: { identity: string; goals: string[]; personality: string };
    rules?: string[];
    skillTreeState?: { selectedNodes: string[]; unlockedNodes: string[] };
  };
}>();
```

### Emits
```typescript
defineEmits<{
  'update:modelValue': [value: AgentConfig];
  change: [value: AgentConfig];
}>();
```

### 子组件整合
- BackendSelector（后端选择+配置）
- SoulEditor（Soul 编辑）
- RulesEditor（规则编辑）
- SkillTreePanel（技能树）

### ⚠️ SkillTree 使用注意
```typescript
import { SkillTree } from '@/stratix-character-creator/core/SkillTree';
// 不要调用 getSelectedNodeIds()，用:
// skillTree.state.selectedNodes
// 或 skillTree.getState().selectedNodes
```

## 验收标准
- [ ] Props/Emits 匹配
- [ ] 整合 4 个子组件
- [ ] 配置变更正确 emit
- [ ] `var(--ds-*)` tokens
- [ ] `npx tsc --noEmit` 通过

## 禁止事项
- 禁止使用 getSelectedNodeIds()
- 禁止导入 Phaser
- 禁止修改 core/
