# T16: AgentChatPanel 组件

## 需求量表自检
- R1: 5 — 对照 V1 ui/AgentChatPanel.ts (426行) + V2 (555行)
- R2: 5 — V1 存在
- R3: 5 — Props/Emits 对照 V2
- R4: 5 — 完整
- R5: 5 — 单一职责
- **总分: 5.0**

## 任务
创建 `v3/components/AgentChatPanel.vue`。

## 上下文

### V1 参考
- `src/stratix-character-creator/ui/AgentChatPanel.ts` (426行)

### Props（对照 V2 AgentChatPanel.vue）
```typescript
defineProps<{
  visible: boolean;
  agentConfig: {
    backendType: string;
    openClawConfig?: any;
    stratixConfig?: any;
    soul?: any;
    rules?: string[];
  };
  characterName?: string;
}>();
```

### Emits
```typescript
defineEmits<{
  complete: [];
  back: [];
}>();
```

### 功能
1. 聊天消息列表
2. 输入框 + 发送按钮
3. 双后端调用（OpenClaw / Stratix）
4. 创建/完成按钮

## 验收标准
- [ ] Props/Emits 匹配
- [ ] 聊天消息渲染
- [ ] 发送交互
- [ ] `var(--ds-*)` tokens
- [ ] `npx tsc --noEmit` 通过

## 禁止事项
- 禁止导入 Phaser
- 禁止修改 core/
