# T06: PartSelector 组件（含体型切换）

## 需求量表自检
- R1: 5 — 对照 V1 ui/PartSelector.ts (270行) + V2 PartSelector.vue (438行)
- R2: 5 — V1 确实存在的核心组件
- R3: 5 — Props/Emits 对照 V2 验证
- R4: 5 — 描述完整
- R5: 4 — 含体型切换，但 V1 就是合并的
- **总分: 4.85**

## 任务
创建 `v3/components/PartSelector.vue` — 部件选择器，包含体型切换。

## 上下文

### Props（对照 V2 PartSelector.vue）
```typescript
defineProps<{
  bodyType: BodyType;
  selectedParts: Record<string, PartSelection>;
}>();
```

### Emits（对照 V2 PartSelector.vue）
```typescript
defineEmits<{
  select: [category: PartCategory, selection: PartSelection];
  deselect: [category: PartCategory];
  randomize: [];
  'update:bodyType': [value: BodyType];
}>();
```

### V1 参考
- `src/stratix-character-creator/ui/PartSelector.ts` (270行)
- 体型切换在 V1 中与 PartSelector 是同一个面板

### 功能
1. 分类标签（body, head, torso, legs, feet, accessories 等）
2. 部件列表（缩略图 + 名称）
3. 变体选择（同一部件不同变体）
4. 体型切换（male/female，放在顶部）
5. 随机化按钮组（三档：最小/标准/全随机）

### 关键依赖
```typescript
import type { BodyType, PartCategory } from '@/stratix-character-creator/constants';
import type { PartSelection, PartMetadata } from '@/stratix-character-creator/types';
import { partRegistry } from '@/stratix-character-creator/core/PartRegistry';
```

## 验收标准
- [ ] Props/Emits 匹配
- [ ] 分类标签切换
- [ ] 部件列表渲染
- [ ] 变体选择
- [ ] 体型切换（male/female）
- [ ] 三档随机化按钮
- [ ] `var(--ds-*)` tokens
- [ ] `npx tsc --noEmit` 通过

## 禁止事项
- 禁止创建独立 BodyTypeSelector 组件（体型切换内嵌）
- 禁止导入 Phaser
- 禁止修改 core/
