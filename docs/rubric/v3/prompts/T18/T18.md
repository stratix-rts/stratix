# T18: 编译验证 + index.ts 切换

## 需求量表自检
- R1: 5 — 最终集成验证
- R2: 5 — 必须做
- R3: 5 — 切换 index.ts
- R4: 5 — 完整
- R5: 5 — 最小范围
- **总分: 5.0**

## 任务
1. 运行 `npx tsc --noEmit` 确保零错误
2. 修改 `index.ts` 切换到 V3
3. 再次编译验证
4. 修复所有编译错误

## 上下文

### index.ts 切换
```typescript
// 从:
export { default } from './v2/CharacterCreatorModal.vue';
// 改为:
export { default } from './v3/CharacterCreatorModal.vue';
```

### 常见编译问题检查清单
1. 类型导入路径是否正确
2. defineProps/defineEmits 泛型语法是否闭合（`<{}>` 不是 `<{}()`)
3. 模板中引用的变量是否在 script setup 中定义
4. 子组件 import 路径是否正确

## 验收标准
- [ ] `npx tsc --noEmit` 零错误
- [ ] index.ts 指向 V3
- [ ] 所有子组件 import 正确
- [ ] 无 `any` 类型滥用

## 禁止事项
- 禁止用 `as any` 绕过类型错误（除非确认安全并注释原因）
- 禁止修改 core/
