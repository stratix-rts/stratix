# T09: CharacterList 组件

## 需求量表自检
- R1: 5 — 对照 V1 ui/CharacterList.ts (327行) + V2 CharacterList.vue (415行)
- R2: 5 — V1 存在
- R3: 5 — Props/Emits 对照 V2
- R4: 5 — 完整
- R5: 5 — 单一职责
- **总分: 5.0**

## 任务
创建 `v3/components/CharacterList.vue`。

## 上下文

### Props（对照 V2 CharacterList.vue）
```typescript
defineProps<{
  characters: SavedCharacter[];
  currentCharacterId?: string;
  isLoading?: boolean;
}>();
```

### Emits（对照 V2）
```typescript
defineEmits<{
  load: [characterId: string];
  delete: [characterId: string];
  setDefault: [characterId: string];
}>();
```

### 功能（来源：V1 ui/CharacterList.ts）
1. 角色列表（缩略图 + 名称 + 创建时间）
2. 加载选中角色
3. 删除（带确认弹窗）
4. 设为默认

## 验收标准
- [ ] Props/Emits 匹配
- [ ] 角色列表渲染
- [ ] 删除确认弹窗
- [ ] 设为默认
- [ ] 加载状态
- [ ] `var(--ds-*)` tokens
- [ ] `npx tsc --noEmit` 通过

## 禁止事项
- 禁止导入 Phaser
- 禁止修改 core/
