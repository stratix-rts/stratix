# T10: JsonEditor 组件

## 需求量表自检
- R1: 5 — 对照 V2 JsonEditor.vue (163行)
- R2: 5 — V2 有，V1 用 JSON 导出
- R3: 5 — Props/Emits 对照 V2
- R4: 5 — 完整
- R5: 5 — 单一职责
- **总分: 5.0**

## 任务
创建 `v3/components/JsonEditor.vue`。

## 上下文

### Props（对照 V2 JsonEditor.vue）
```typescript
defineProps<{
  visible: boolean;
  parts: Record<string, PartSelection>;
}>();
```

### Emits（对照 V2）
```typescript
defineEmits<{
  close: [];
  save: [parts: Record<string, PartSelection>];
}>();
```

### 功能
1. 弹窗式 JSON 编辑器
2. 格式校验
3. 保存/取消

## 验收标准
- [ ] Props/Emits 匹配
- [ ] JSON 格式校验
- [ ] 弹窗开关
- [ ] `var(--ds-*)` tokens
- [ ] `npx tsc --noEmit` 通过

## 禁止事项
- 禁止导入 Phaser
- 禁止修改 core/
