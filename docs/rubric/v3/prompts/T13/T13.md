# T13: RulesEditor 组件

## 需求量表自检
- R1: 5 — 对照 V1 ui/RulesEditor.ts (304行)
- R2: 5 — V1 存在
- R3: 5 — 简单 string[] 接口
- R4: 5 — 完整
- R5: 5 — 单一职责
- **总分: 5.0**

## 任务
创建 `v3/components/RulesEditor.vue`。

## 上下文

### V1 参考
- `src/stratix-character-creator/ui/RulesEditor.ts` (304行)

### SavedCharacter.rules 字段（来源：types/index.ts 行 87）
```typescript
rules?: string[];  // 规则列表
```

### Props
```typescript
defineProps<{
  modelValue?: string[];
}>();
```

### Emits
```typescript
defineEmits<{
  'update:modelValue': [value: string[]];
}>();
```

### 功能
1. 规则列表（每条一行，可添加/删除）
2. 新增规则输入框
3. 拖拽排序（可选）

## 验收标准
- [ ] Props/Emits 正确
- [ ] 规则添加/删除
- [ ] `var(--ds-*)` tokens
- [ ] `npx tsc --noEmit` 通过

## 禁止事项
- 禁止导入 Phaser
- 禁止修改 core/
