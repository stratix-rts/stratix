# T05: usePartSelection composable

## 需求量表自检
- R1: 5 — 对照 V2 usePartSelection.ts (394行) + V1 Scene 内随机化逻辑
- R2: 5 — 核心状态管理
- R3: 4 — 接口基于 V2，对照 V1 验证
- R4: 4 — 三档随机化需要仔细实现
- R5: 5 — 单一职责
- **总分: 4.85**

## 任务
创建 `v3/composables/usePartSelection.ts`。

## 上下文

### V2 参考（来源：`src/components/character-creator/v2/composables/usePartSelection.ts`，394 行）

### 关键依赖
```typescript
import type { Ref } from 'vue';
import type { BodyType, PartCategory } from '@/stratix-character-creator/constants';
import type { PartSelection, PartMetadata, SavedCharacter } from '@/stratix-character-creator/types';
import { partRegistry } from '@/stratix-character-creator/core/PartRegistry';
```

### 返回接口（对照 V2 行 371-394）
```typescript
return {
  // 状态
  bodyType,           // Ref<BodyType>
  selectedParts,      // Ref<Record<string, PartSelection>>
  availableParts,     // ComputedRef<Map<PartCategory, PartMetadata[]>>

  // 操作
  selectPart,         // (category: PartCategory, selection: PartSelection) => void
  deselectPart,       // (category: PartCategory) => void
  setBodyType,        // (bodyType: BodyType) => void

  // 随机化
  randomize,          // (mode: 'minimal' | 'normal' | 'full') => void
};
```

### 三档随机化（来源：V1 Scene + V2 usePartSelection）
- **minimal**: 只换头和衣服，保留身体
- **normal**: 所有部件随机，可选部件 50% 留空
- **full**: 全随机，不跳过任何类别

### 构造参数
```typescript
export function usePartSelection(character: Ref<SavedCharacter | null>) {
  // 从 character.value.bodyType / character.value.parts 初始化
}
```

## 验收标准
- [ ] 接口与 V2 完全一致
- [ ] 三档随机化正确实现
- [ ] bodyType 切换时保留兼容部件
- [ ] availableParts 响应 bodyType 变化
- [ ] `npx tsc --noEmit` 通过

## 禁止事项
- 禁止修改 core/
- 禁止添加 V2 中不存在的导出
