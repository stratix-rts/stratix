# T11: BackendSelector 组件

## 需求量表自检
- R1: 5 — 对照 V1 ui/BackendSelector.ts (628行) + V2 (541行)
- R2: 5 — V1 存在
- R3: 5 — Props/Emits 对照 V2
- R4: 5 — 完整
- R5: 4 — 后端选择+配置较复杂但必要
- **总分: 4.85**

## 任务
创建 `v3/components/BackendSelector.vue`。

## 上下文

### Props（对照 V2 BackendSelector.vue）
```typescript
defineProps<{
  modelValue?: {
    backendType: AgentBackendType;
    openClawConfig?: OpenClawConfigLocal;
    stratixConfig?: StratixDirectConfig;
  };
}>();
```

### 类型来源确认
```typescript
import type { OpenClawConfigLocal } from '@/stratix-character-creator/types';
import type { StratixDirectConfig } from '@/stratix-core/stratix-protocol';
type AgentBackendType = 'openclaw' | 'stratix' | 'direct';
```

**⚠️ 检查 AgentBackendType 是否存在于代码库中：**
```bash
grep -rn "AgentBackendType" src/stratix-character-creator/ src/stratix-core/
```
如果不存在，定义在组件内部：
```typescript
type AgentBackendType = 'openclaw' | 'stratix' | 'direct';
```

### Emits
```typescript
defineEmits<{
  'update:modelValue': [value: { backendType: AgentBackendType; openClawConfig?: OpenClawConfigLocal; stratixConfig?: StratixDirectConfig }];
  change: [value: { backendType: AgentBackendType; openClawConfig?: OpenClawConfigLocal; stratixConfig?: StratixDirectConfig }];
}>();
```

### 功能（来源：V1 ui/BackendSelector.ts）
1. 后端类型选择（OpenClaw / Stratix / Direct）
2. OpenClaw 配置表单（endpoint, accountId, apiKey）
3. Stratix 配置表单
4. 连接测试按钮

## 验收标准
- [ ] Props/Emits 匹配
- [ ] 三种后端类型切换
- [ ] 配置表单
- [ ] `var(--ds-*)` tokens
- [ ] `npx tsc --noEmit` 通过

## 禁止事项
- 禁止导入 Phaser
- 禁止修改 core/
- 禁止假设类型存在（不确定就 grep 确认）
