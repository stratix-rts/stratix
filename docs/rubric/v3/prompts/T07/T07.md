# T07: AnimationControls 组件

## 需求量表自检
- R1: 5 — 对照 V2 AnimationControls.vue (382行)
- R2: 5 — V1 有动画控制按钮
- R3: 5 — Props/Emits 对照 V2 验证
- R4: 5 — 描述完整
- R5: 5 — 单一职责
- **总分: 5.0**

## 任务
创建 `v3/components/AnimationControls.vue`。

## 上下文

### Props（对照 V2 AnimationControls.vue）
```typescript
defineProps<{
  animation: AnimationName;
  direction: number;
  scale: number;
  isPlaying: boolean;
}>();
```

### Emits（对照 V2）
```typescript
defineEmits<{
  'set-animation': [value: AnimationName];
  'set-direction': [value: number];
  'set-scale': [value: number];
  'toggle-play': [];
}>();
```

### 功能
1. 动画选择按钮组（idle, walk, run, slash 等）
2. 方向按钮（4 方向）
3. 缩放 +/- 按钮
4. 播放/暂停按钮

### 动画列表（来源：V1 constants.ts）
```typescript
type AnimationName = 'idle' | 'walk' | 'run' | 'spellcast' | 'thrust' | 'slash' | 'shoot' | 'hurt' | 'climb' | 'jump' | 'sit' | 'emote' | 'combat_idle' | 'backslash' | 'halfslash';
```

常用动画（按钮组显示）：idle, walk, run, slash, shoot, spellcast, combat_idle

## 验收标准
- [ ] Props/Emits 匹配 V2
- [ ] 动画按钮组
- [ ] 方向选择
- [ ] 缩放控制
- [ ] 播放/暂停
- [ ] `var(--ds-*)` tokens
- [ ] `npx tsc --noEmit` 通过

## 禁止事项
- 禁止导入 Phaser
- 禁止修改 core/
