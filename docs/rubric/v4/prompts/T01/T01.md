# T01: 创建 v4 目录结构 + types.ts

## 需求量表自检
- R1 (来源可追溯): 5 — types.ts 中的类型全部来自 `src/stratix-character-creator/types/index.ts`
- R2 (必要性): 5 — 目录结构是基础设施
- R3 (接口真实性): 5 — CreatorStep 来自 V1 types
- R4 (完整性): 5 — 只需创建目录和类型文件
- R5 (最小范围): 5 — 最小任务
- **加权总分: 5.0**

## 任务
创建 v3 目录结构 + types.ts 类型文件。

## 上下文

### V1 类型定义（来源：`src/stratix-character-creator/types/index.ts` 行 162）
```typescript
export type CreatorStep = 'appearance' | 'openclaw' | 'agent';
```

### V4 types.ts 应导出
```typescript
// 直接 re-export from V1
export type { CreatorStep } from '@/stratix-character-creator/types';
```

不需要在 types.ts 中定义新类型。所有业务类型都从 `@/stratix-character-creator/types` 导入。

### 目录结构
```
src/components/character-creator/v4/
├── CharacterCreatorModal.vue   ← 空文件，T02 填充
├── components/                 ← 空目录
├── composables/                ← 空目录
└── types.ts                    ← re-export CreatorStep
```

### index.ts 切换
开发期间 **不修改** `src/components/character-creator/index.ts`（保持指向 V2）。T18 完成时切换。

## 验收标准
- [ ] v4/ 目录存在
- [ ] v4/components/ 目录存在
- [ ] v4/composables/ 目录存在
- [ ] types.ts 正确 re-export CreatorStep
- [ ] `npx tsc --noEmit` 通过
- [ ] index.ts 未被修改（仍指向 V2）

## 禁止事项
- 禁止修改 index.ts
- 禁止在 types.ts 中定义新类型
- 禁止创建任何 .vue 文件（T02 开始）


---

## 美学约束（A1-A8）— 必须遵守

### A1 间距规律性
- 使用 Spacing token（4, 8, 16, 24, 32, 48, 64），禁止硬编码 margin/padding
- 同层级元素间距一致
- 嵌套越深内间距越小（页面 32-48px → 区块 24px → 组件 12-16px → 元素 4-8px）

### A2 布局对齐
- flex/grid 布局，禁止 position: absolute 做正常布局
- 对齐属性必须显式声明（justify-content, align-items）

### A3 色彩与风格统一
- 颜色用 var(--ds-*)，禁止硬编码色值
- 同类元素相同样式
- 字号用 Typography token（xs=10, sm=12, md=14, lg=16, xl=20, 2xl=24, 3xl=32）
- 圆角用 Radii token（sm=2, md=4, lg=8, xl=16）
- 主色不超过 3 种

### A4 比例与尺寸
- 主要容器用经典比例（1:1.618 / 1:1.414 / 16:9 / 4:3 / 1:1）
- 图片/卡片声明 aspect-ratio
- 同类元素等尺寸，尺寸用 8px 基准

### A5 视觉层级与阅读流
- 字号层级 ≤ 3 层，字重配合层级（700→400→300）
- 1 个视觉焦点
- 阅读流：重要信息左上，CTA 在末端

### A6 简洁性
- CSS 选择器深度 ≤ 3
- 单组件样式属性 ≤ 15 条
- 重复样式提取共享 class

### A7 信息密度
- 留白 30-50%
- 单区域 ≤ 3 种信息类型
- 行间距 ≥ 字号 1.5 倍

### A8 对称与结构
- 表单标签统一方向
- 网格等宽等高
- 非对称需功能性理由
