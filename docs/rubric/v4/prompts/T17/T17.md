# T17: EventBus + 数据流串联

## 需求量表自检
- R1: 5 — 对照 V1 CharacterCreatorScene.ts 的 EventBus 使用
- R2: 5 — 集成必须
- R3: 5 — 事件名对照 V1
- R4: 5 — 完整
- R5: 4 — 集成任务
- **总分: 4.85**

## 任务
在 `CharacterCreatorModal.vue` 中集成 EventBus 和完整数据流。

## 上下文

### V1 EventBus（来源：`src/stratix-character-creator/core/EventEmitter.ts`）
```typescript
import { eventEmitter } from '@/stratix-character-creator/core/EventEmitter';
```

### 外部打开创建器（来源：V1 Scene + App.vue）
```typescript
// App.vue 通过 uiStore.showCharacterCreator 控制显示
// editCharacterId 用于编辑已有角色
```

### 数据流（来源：V1 Scene）
```
App.vue visible/editCharacterId
  → CharacterCreatorModal
    → useCharacterState (CRUD)
    → usePartSelection (部件选择)
    → usePreviewControl (动画控制)
    → 各子组件
      → 子组件 emit → 主容器处理 → composable 更新
```

### 关键事件监听（来源：V1 CharacterCreatorScene.ts）
- `character:save` → 保存当前角色
- `character:delete` → 删除角色
- `character:select` → 切换角色

### handleSave 流程
```typescript
async function handleSave() {
  const success = await charState.saveCharacter();
  if (success) {
    showToast('角色已保存', 'success');
    emit('created', charState.currentCharacter.value!); // 或 'updated'
  } else {
    showToast('保存失败', 'error');
  }
}
```

## 验收标准
- [ ] 外部打开创建器正常
- [ ] 保存 → emit('created'/'updated')
- [ ] 删除 → emit('deleted')
- [ ] 关闭 → emit('close')
- [ ] 编辑已有角色正常
- [ ] `npx tsc --noEmit` 通过

## 禁止事项
- 禁止修改 core/
- 禁止修改 index.ts


---

## 美学约束（A1-A8）— 必须遵守

### A1 间距规律性
- 使用 Spacing token（4, 8, 16, 24, 32, 48, 64），禁止硬编码 margin/padding
- 同层级元素间距一致
- 嵌套越深内间距越小（页面 32-48px → 区块 24px → 组件 12-16px → 元素 4-8px）

### A2 布局对齐
- flex/grid 布局，禁止 position: absolute 做正常布局
- 对齐属性必须显式声明（justify-content, align-items）

### A3 色彩与风格统一
- 颜色用 var(--ds-*)，禁止硬编码色值
- 同类元素相同样式
- 字号用 Typography token（xs=10, sm=12, md=14, lg=16, xl=20, 2xl=24, 3xl=32）
- 圆角用 Radii token（sm=2, md=4, lg=8, xl=16）
- 主色不超过 3 种

### A4 比例与尺寸
- 主要容器用经典比例（1:1.618 / 1:1.414 / 16:9 / 4:3 / 1:1）
- 图片/卡片声明 aspect-ratio
- 同类元素等尺寸，尺寸用 8px 基准

### A5 视觉层级与阅读流
- 字号层级 ≤ 3 层，字重配合层级（700→400→300）
- 1 个视觉焦点
- 阅读流：重要信息左上，CTA 在末端

### A6 简洁性
- CSS 选择器深度 ≤ 3
- 单组件样式属性 ≤ 15 条
- 重复样式提取共享 class

### A7 信息密度
- 留白 30-50%
- 单区域 ≤ 3 种信息类型
- 行间距 ≥ 字号 1.5 倍

### A8 对称与结构
- 表单标签统一方向
- 网格等宽等高
- 非对称需功能性理由
